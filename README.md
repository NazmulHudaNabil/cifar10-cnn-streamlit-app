# cifar10-cnn-streamlit-app
A deep learning web application that classifies images into 10 categories using a trained CNN model on the CIFAR-10 dataset, built with TensorFlow and deployed using Streamlit.
